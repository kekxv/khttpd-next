load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "app",
    srcs = [
        "HelloController.hpp",
        "HelloStreamController.hpp",
        "HelloWsController.hpp",
        "main.cpp",
    ],
    deps = [
        "@khttpd",
        "@spdlog//:spdlog",
    ],
)
